mixin bookCard(name, source, description, info, href, target="_self")
    a(href=href, target=target).book-card
        div.book-card__name=name
        div.book-card__info=info
        //div.book-card__description=description
        div.book-card__source=source